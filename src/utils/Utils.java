package utils;

import java.time.format.DateTimeFormatter;

public class Utils {
    // Formato padrão para exibição de data e hora
    public static final DateTimeFormatter formatter = DateTimeFormatter.ofLocalizedPattern("dd/MM/yyyy HH:mm");
}